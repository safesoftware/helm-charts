apiVersion: v1
kind: Service
metadata:
  labels:
    safe.k8s.fmeserver.build: {{ .Values.fmeserver.buildNr }}
    safe.k8s.fmeserver.component: fmeserverqueue
  name: fmeserverqueue
spec:
  ports:
  - name: redis
    port: 6379
  clusterIP: None
  selector:
    safe.k8s.fmeserver.component: fmeserverqueue

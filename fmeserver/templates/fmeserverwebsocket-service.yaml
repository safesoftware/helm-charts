apiVersion: v1
kind: Service
metadata:
  labels:
    safe.k8s.fmeserver.build: {{ .Values.fmeserver.buildNr }}
    safe.k8s.fmeserver.component: fmeserverwebsocket
  name: fmeserverwebsocket
spec:
  ports:
  - name: websocket
    port: 7078
  clusterIP: None
  selector:
    safe.k8s.fmeserver.component: fmeserverwebsocket

apiVersion: v1
kind: Service
metadata:
  labels:
    safe.k8s.fmeserver.component: db
    {{- include "fmeserver.common.labels" . | indent 4 }}
  name: fmeserverdb
spec:
  ports:
  - name: pg
    port: 5432
  clusterIP: None
  selector:
    safe.k8s.fmeserver.component: db

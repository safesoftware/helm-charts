apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    safe.k8s.fmeserver.build: {{ .Values.fmeserver.buildNr }}
    safe.k8s.fmeserver.component: misc
  name: fmeserver-logs-{{ .Values.fmeserver.buildNr }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: {{.Values.storage.logs.class}}
  resources:
    requests:
      storage: {{.Values.storage.logs.size}}
